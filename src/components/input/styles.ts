import styled from "styled-components";

const StyledInput = styled.div`
  display: flex;
  flex-direction: column;
`;
const BillInput = styled.input``;
const PersonInput = styled.input``;

export { StyledInput, BillInput, PersonInput };
