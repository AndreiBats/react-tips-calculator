import { StyledInput, BillInput, PersonInput } from "./styles";

export const Input = () => {
  return (
    <StyledInput>
      <BillInput type="text" placeholder="Enter bill" />
      <PersonInput type="text" placeholder="Enter bill" />
    </StyledInput>
  );
};
