import { Colors } from "../../types";

export const CustomStyles = {
  control: () => ({
    width: "100%",
    display: "flex",
    background: Colors.White,
    borderRadius: "30px",
    marginBottom: "45px",
    padding: "15px",
  }),
};
