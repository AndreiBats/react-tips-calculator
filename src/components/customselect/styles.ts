import { Color } from "../../types";

export const CustomStyles = {
  control: () => ({
    width: "100%",
    display: "flex",
    background: Color.White,
    borderRadius: "30px",
    marginBottom: "45px",
    padding: "15px",
  }),

  indicatorSeparator: () => ({
    width: 0,
  }),
};
